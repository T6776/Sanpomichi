<% if course.favorited_by?(current_user) %>
  <p>
    <%= link_to course_favorites_path(course), method: :delete, remote: true do %>
      ♥<%= course.favorites.count %> いいね
    <% end %>
  </p>
  <% else %>
  <p>
    <%= link_to course_favorites_path(course), method: :post, remote: true do %>
      ♡<%= course.favorites.count %> いいね
    <% end %>
  </p>
<% end %>