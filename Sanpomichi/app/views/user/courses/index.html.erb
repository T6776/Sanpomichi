 <div class= "container">

  <h1>コース一覧</h1>

  <div class= "col">
    <% @courses.each do |course| %>
      <%= course.name %><br>
      <%= course.prefecture %><br>
      <%= link_to "詳細へ", course_path(course) %><br>
    <% end %>
  </div>

</div>